let processImage = (ws, data, type)=>{
 console.log(ws, data, type);
};

let saveImage = (data)=>{
 console.log(data);
};

module.exports = {
    "processImage":processImage,
    "saveImage":saveImage
}