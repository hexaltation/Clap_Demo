# TODO

## Tasks

- Create a pid mechanisme hash(UserAgent+IP+TimeStamp) [X]
- Upload source file to server
  - Convert file to byteArray [X]
  - create src image from recieved byteArray
- Create line ffmpeg to use complex filter color level
  - Create mechanism to produce thumb
  - Create mechanism to produce full render [X]
- Image thumb from server to canvas
  - Create canvas in display limits keeping ratio of src image.
  - Send ratio to backend 
- Download Mechanism
  - on download websocket send back PID based download link
  - Front make an xrh call on the address.
- Purge mechanism on ws close
- Write install.sh for initiat the project after cloning